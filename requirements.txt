# PaddlePaddle (CPU version by default)
paddlepaddle>=2.6.0; sys_platform == 'linux'
# For Windows local testing, paddlepaddle is also available via pip

# PaddleOCR
paddleocr>=2.8.0

# API Framework
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
pydantic>=2.6.0

# Image processing
opencv-python-headless>=4.9.0.80
Pillow>=10.2.0
numpy>=1.24.0,<2.0.0
